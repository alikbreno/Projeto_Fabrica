export type MenuTreeType = {
  id: string;
  title: string;
  href: string;
  children?: MenuTreeType[];
  // navlabel?: boolean;
  // subheader?: string;
  // chip?: string;
  // chipColor?: any;
  // variant?: string | any;
  // external?: boolean;
  // level?: number;
  // isAdm?: boolean;
}